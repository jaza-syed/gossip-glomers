# Quickstart

`mise install` to get go and maelstrom available. `go build` in any subfolder to build solution. Fly.io test snippets need to be adapted slightly for `maelstrom` binary on path and locations of go build artifacts.
